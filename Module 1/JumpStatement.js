// Input num valus is in variable *num*
var num = parseInt(readLine().trim());
function printOddNumbers(N) {
    for (let i = 0; i < N; i++) {
        if (i % 2 !== 0) {
        console.log(i);
    }
}
}

//Sample Input


// Function Call
printOddNumbers(num);
